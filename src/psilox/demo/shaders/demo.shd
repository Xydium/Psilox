VERTEX:

#version 120

void main() {
	gl_Position = projection_transform * gl_Vertex;
}

FRAGMENT:

#version 120

uniform vec4 color;
uniform float time;

void main() {
	gl_FragColor = vec4(color.xyz, max(sin(time + gl_FragCoord.x / 10), cos(time + gl_FragCoord.y / 10)));
}